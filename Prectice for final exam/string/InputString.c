#include <stdio.h>

int main() {
    // code here
    char name[20];
    printf("Enter name: ");
    // scanf("%s", name);
        gets(name); 
        puts(name);
    // printf("Your name is %s.", name);
    return 0;
}